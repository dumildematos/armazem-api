server:
  error:
    include-message: always
    include-binding-errors: always
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/armazem"
    username: "root"
    password: ""
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: create